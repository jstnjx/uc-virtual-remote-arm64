Package ships no license file; `license-checker` falls back to its README, which contains no
license text either.

Text taken verbatim from the upstream monorepo, which now hosts this package:
https://github.com/TooTallNate/node-https-proxy-agent/blob/main/packages/https-proxy-agent/LICENSE

The generator's GitHub lookup only probes the repository root, so it cannot find a license
that lives under `packages/<name>/`.
