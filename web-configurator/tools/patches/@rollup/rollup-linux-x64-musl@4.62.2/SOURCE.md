Platform-specific Rollup binary package. It ships no license file, and the generator's GitHub
lookup needs an authenticated `gh` — which CI does not have — so the license has to be
committed.

Same text as the `@rollup/rollup-darwin-arm64` sibling already in this directory: both are
published from https://github.com/rollup/rollup at the same version, under the same MIT license.
