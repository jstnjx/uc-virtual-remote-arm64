Platform-specific esbuild binary package. It ships no license file, and the generator's GitHub
lookup needs an authenticated `gh` — which CI does not have — so the license has to be committed.

Same text as the `@esbuild/darwin-arm64` sibling already in this directory: both are published
from https://github.com/evanw/esbuild at the same version, under the same MIT license and
copyright holder.
